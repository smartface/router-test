(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['module', 'exports', './constants'], factory);
  } else if (typeof exports !== "undefined") {
    factory(module, exports, require('./constants'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod, mod.exports, global.constants);
    global.findClassNames = mod.exports;
  }
})(this, function (module, exports, _constants) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });


  var findClassNames = function findClassNames() {
    var classesRegExp = /[\.\#]?([a-zA-Z\W0-9][^\.^\#]*)/g;
    var cache = {};

    return function styleSelector(selector) {
      if (Object.prototype.hasOwnProperty.call(cache, selector)) {
        return cache[selector];
      }

      var classes = selector.replace(/[ ]+/g, ' ').split(' ').map(function (items) {
        return items.match(classesRegExp);
      });

      if (!classes) {
        return '';
      }

      cache[selector] = classes;

      return classes;
    };
  }();

  exports.default = findClassNames;
  module.exports = exports['default'];
});